import { styled } from 'styled-components';

export const ErrorWrapper = styled.div`
  border: 3px solid red;
  padding: 1rem;
  border-radius: 15px;
  line-height: 25px;
  font-weight: bold;
  font-size: 1.4rem;
`;
